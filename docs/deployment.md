# Deployment

This project uses Vercel's native GitHub integration for deployment.
When a push to the `main` branch occurs, Vercel automatically builds and deploys the application.

No custom `.github/workflows/deploy.yml` is required.
